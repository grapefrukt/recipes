baseurl: /recipes

collections:
  recipes:
    output: true

defaults:
  - scope:
      path: "" # an empty string here means all files in the project
      type: "recipes"
    values:
      layout: "recipe"
      tags: ["övrigt"]
      permalink: /:slug.html
